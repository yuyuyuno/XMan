<!DOCTYPE html>
<html>
  <head>
    <title>XMan</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    <link rel="stylesheet" href="https://bootswatch.com/4/cyborg/bootstrap.min.css">
  
    <style>
      h1 {
        color: #0275d8;
      }
      body {
        margin-bottom: 40px;
      }
      footer {
        height: 40px;
        position: absolute;
        bottom: 0;
        width: 100%;
      }
      .my-primary {
        background-color: #0275d8;
        color: white;
      }
      a:link, a:visited {
        color: white;
      }
      .a-primary {
        text-decoration: underline;
      }
      .a-primary:hover {
        color: #0275d8;
      }


    </style>

  </head>

  <body>
    <div>

      <header>
        <nav class="navbar navbar-expand-lg my-primary">
          <div class="navbar-brand" >
            <img src="/images/logo1.jpg" width="30" height="30" class="d-inline-block aling-top" alt="Logo">
            <%= link_to 'XMan - Your expense manager', root_path %>
          </div>
          <div class="navbar-right">
            <%= link_to t('logout'), session_logout_path %>
          </div>
        </nav>
      </header>

      <div class="container">
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
        <%= yield %>
      </div>

      <footer class="bg-secondary">
          <button class="btn btn-link js-switch-locale" data-locale="ru">Ru</button>
          <button class="btn btn-link js-switch-locale" data-locale="en">En</button>
      </footer>

    </div>
  </body>
  </html>
  

